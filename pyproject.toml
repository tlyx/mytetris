[project]
name = "mytetris"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame-ce>=2.5.7",
]

[tool.basedpyright]
reportUnusedCallResult = false  # 关闭未使用返回值警告
reportAny = false
reportUnusedVariable = { "exclude" = ["^_"] }
